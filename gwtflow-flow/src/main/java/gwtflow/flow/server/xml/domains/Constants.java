package gwtflow.flow.server.xml.domains;

public class Constants {
    public enum DATA {
        UIControlType,
        UIRendered,
        UICssClass,
        UIReadonly,
        UIHidden,
        CMDName,
        CMDLabel,
        PNTViewName
    }

//    public enum KEY {
//        dCmd, dProp, dView
//    }

    public enum ATTR_NAME {command, property, view}

    public enum POINT_TYPE {
        point, view, cmd, nested
    }
}
